@model ECommerceOMS.Models.Customer
@{ ViewData["Title"] = "Delete Customer"; }

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow border-danger">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0"><i class="bi bi-exclamation-triangle"></i> Delete Customer</h4>
            </div>
            <div class="card-body">
                <div class="alert alert-danger">
                    Are you sure you want to delete this customer? This will also delete all their orders.
                </div>
                <table class="table table-borderless">
                    <tr><td class="fw-bold">Name:</td><td>@Model.FullName</td></tr>
                    <tr><td class="fw-bold">Email:</td><td>@Model.Email</td></tr>
                    <tr><td class="fw-bold">Phone:</td><td>@Model.Phone</td></tr>
                </table>
                <form asp-action="Delete" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash"></i> Yes, Delete
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-x-circle"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>